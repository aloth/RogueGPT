[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "roguegpt"
version = "1.4.0"
description = "A controlled stimulus generation framework for AI news authenticity research"
readme = "README.md"
license = { text = "GPL-3.0-or-later" }
authors = [
    { name = "Alexander Loth", email = "alexander.loth@stud.fra-uas.de" },
    { name = "Martin Kappes" },
    { name = "Marc-Oliver Pahl", email = "marc-oliver.pahl@imt-atlantique.fr" },
]
keywords = [
    "fake-news",
    "misinformation",
    "LLM",
    "stimulus-generation",
    "NLP",
    "research",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
requires-python = ">=3.10"
dependencies = [
    "pymongo>=4.0",
    "dnspython>=2.0",   # required for mongodb+srv:// URIs
]

[project.optional-dependencies]
app = [
    "streamlit>=1.30",
    "openai>=1.0",
]
mcp = [
    # mcp.server.fastmcp does not exist before 1.2; mcp 2.x renamed FastMCP.
    "mcp[cli]>=1.2,<2",
]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-mock>=3.0",
]

[project.scripts]
roguegpt = "cli:main"

[project.urls]
Homepage       = "https://github.com/aloth/RogueGPT"
Repository     = "https://github.com/aloth/RogueGPT"
"Bug Tracker"  = "https://github.com/aloth/RogueGPT/issues"
Documentation  = "https://github.com/aloth/RogueGPT#readme"
DOI            = "https://doi.org/10.5281/zenodo.20681920"

# ── Flat-layout: include all top-level .py modules ──────────────────────────
# prompt_engine.json is shipped via data-files rather than package-data: this is a
# flat layout with no package directory, so there is no package for package-data to
# attach the file to and it was silently dropped from the wheel. core.py resolves it
# through importlib.resources with a source-checkout fallback.
[tool.setuptools]
py-modules = ["core", "cli", "mcp_server", "app", "generate_batch"]
include-package-data = false

[tool.setuptools.data-files]
"share/roguegpt" = ["prompt_engine.json"]

# ── pytest ───────────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
testpaths    = ["tests"]
addopts      = "-v --tb=short"
markers      = [
    "requires_db: marks tests that need a live MongoDB instance (deselect with -m 'not requires_db')",
]

# ── coverage ─────────────────────────────────────────────────────────────────
[tool.coverage.run]
source = ["."]
omit   = ["tests/*", "app.py", "generate_batch.py", "venv/*", ".venv/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise RuntimeError",
]
